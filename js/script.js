window.onload = function () {
  // const bubbles = document.querySelectorAll('.thought img')
  // console.log(bubbles)
  /*
  bubbles.forEach(thought => {
    thought.addEventListener('mouseover', (e) => {
      e.target.
    })
  })
*/
  // main.style.height = window.innerHeight - 100 + 'px'
}
